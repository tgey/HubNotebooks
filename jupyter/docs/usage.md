# Install Medicision

```bash
# Clone this repository
$ git clone https://github.com/tgey/Medicision

# Go into the repository
$ cd Medicision

# Download all data from GoogleDrive
$ cd data
$ python googledrive.py

# Install docker images
$ docker-compose build

# Run the project
$ docker-compose up -d
```
